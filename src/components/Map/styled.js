import styled from 'styled-components';
import MapView, {PROVIDER_GOOGLE} from 'react-native-maps';

export const MapContainer = styled(MapView)`
  height: 100%;
  width: 100%;
`;

export const MapMarker = styled(MapView.Marker)``
